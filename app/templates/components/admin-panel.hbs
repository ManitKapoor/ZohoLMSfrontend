<div class="container-fluid">
  <div class="row">
    <div class="col-md-4 col-lg-4">

      {{!-- {{admin menu}} --}}
   	 <div class="panel panel-info">
       <div class="panel panel-body">
         <ul class="nav nav-pills nav-stacked">
           <li class="active"><a {{action 'setopt' 'addUser' on="click"}}>Add User</a></li>
           <li class="active"><a {{action 'setopt' 'addBook' on="click"}}>Add Book</a></li>
           <li class="active"><a {{action 'setopt' 'viewUser' on="click"}}>View Users</a></li>
         </ul>
       </div>
      </div>

     </div>
     <div class="col-md-8 col-lg-8">

      {{!-- {{option}} --}}
     {{#if addUser}}
       {{add-user}}
      {{/if}}
     {{#if addBook}}
       {{add-book}}
      {{/if}}
     {{#if viewUser}}
       {{view-user}}
      {{/if}}  

     </div>
  </div>
</div>