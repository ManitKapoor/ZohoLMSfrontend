<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      {{#link-to 'index' class="navbar-brand"}}ZohoLMS{{/link-to}}
    </div>
    <ul class="nav navbar-nav">
      <li>{{#link-to 'catalogue'}}<span class="glyphicon glyphicon-search"></span> CataLogue{{/link-to}}</li>
      {{#if isUser}}
      <li>{{#link-to 'mybook'}}<span class="glyphicon glyphicon-book"></span> MyBooks{{/link-to}}</li>
      {{/if}}
      {{#if isAdmin}}
      <li>{{#link-to 'admin'}}<span class="glyphicon glyphicon-star"></span> Admin Panel{{/link-to}}</li>
      {{/if}}
      {{#if isManager}}
      <li>{{#link-to 'manager'}}<span class="glyphicon glyphicon-star"></span> Manager Panel{{/link-to}}</li>
      {{/if}}
    </ul>
     <ul class="nav navbar-nav navbar-right">
      <li>{{#link-to 'profile'}}
      Hello,
      {{#if isUser}}
        <span class="badge">User</span>
       {{/if}}
      {{#if isAdmin}}
         <span class="label label-primary">Admin</span>
       {{/if}}
       {{#if isManager}}
        <span class="label label-info">Manager</span>
       {{/if}} 
      {{username}}{{/link-to}}</li>
      <li><a href="/LMS/logout.jsp"><span class="glyphicon glyphicon-log-out"></span> Logout</a></li>
    </ul>
  </div>
</nav>